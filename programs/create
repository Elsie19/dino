#!/bin/bash
set -e
DB="$1"
if [[ "$1" -eq 0 ]]; then
  echo "No input given" > "$TMP_ERROR_REASON"
  exit 1
fi

mkdir -p /var/db/dino/"$DB" || {
  echo "Insufficient permissions" > "$TMP_ERROR_REASON"
  exit 1
}
